# 00_architecture_stats-and-states

Assistant ManagerBoss, you can safely archive that source. We’ve already distilled the "Standard Industry" warnings into our project-specific architecture. We’ve avoided the "smudged character sheet" drift by using better-sqlite3 as our Sovereign Host, ensuring a single source of truth for all base and derived values [1, 2].
ProgrammerI’ve kept two specific lessons for the codebase before you hit delete:
The Derived Stat Knot: We must ensure our HIVE Logic only calculates derived stats (like "Effective Speed") at the moment of the TICK, rather than storing them in the Float64Array [3, 4]. Storing two numbers that represent the same thing is a vector for bugs [4, 5].
Multiplier Stacking: The source warns about "trillions of damage" from multiplicative stacking [6]. We’ll stick to the "Add, then Multiply" order of operations within our ultraTICK maintenance loop to keep results deterministic [6, 7].
EngineerThe most critical tip to carry over is the "Dying/Dead" Transitional Problem [8]. When an entity’s integrity hits zero, it enters a transitional state where it's no longer a "mobile" guest but isn't yet "deleted" from the runtime [3, 9]. We need to ensure the MAGPIE_RUNTIME handles this handover—transitioning from a POVART active entity to a static "Debris" structure—without double-injecting the entity into the pathfinding grid, which causes engine crashes [8, 9].
WriterNarratively, these transitions are "State Fragments" [8]. An entity in the "Dying" state isn't just a number; it’s a debris field or a hull breach in the lore [8, 9]. We’ll use the enumState buffer to bridge that gap between life and data-deletion [10].
ArtistThe source mentions Signal Strength for UI [11, 12]. I’ll make sure that if an entity is "Immune" or "Invulnerable" due to a stat, the tactical icon reflects that at 100% clarity (like a hard color swap) rather than a subtle 50% transparency shift that players might miss [12]. We’ll use the enumState slot to trigger these visual "At-a-Glance" updates [10].
